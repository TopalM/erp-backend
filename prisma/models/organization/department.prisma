model Department {
  id          String @id @default(cuid())

  name        String
  code        String @unique

  users       User[]
  employees   Employee[]

  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@index([name])
  @@map("Department")
}