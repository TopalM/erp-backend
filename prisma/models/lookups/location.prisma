model Location {
  id          Int @id

  name        String @unique

  isActive    Boolean @default(true)

  employees   Employee[]

  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  @@map("Lookup_Location")
}