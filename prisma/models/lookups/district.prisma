model District {
  id          Int @id

  cityId      Int

  value       String

  city        City @relation(fields: [cityId], references: [id], onDelete: Restrict)

  employees   Employee[]

  @@index([cityId])
  @@map("Lookup_District")
}