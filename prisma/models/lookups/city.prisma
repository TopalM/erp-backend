model City {
  id            Int @id

  countryId     Int

  value         String 

  country       Country @relation(fields: [countryId], references: [id], onDelete: Restrict)

  districts     District[]
  
  taxOffices    TaxOffice[]

  employees     Employee[]

  @@index([countryId])
  @@map("Lookup_City")
}